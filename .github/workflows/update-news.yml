name: Auto News Update

on:
  schedule:
    - cron: '0 * * * *'  # اجرای هر ساعت
  workflow_dispatch:     # اجرا دستی

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.x'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run scraper
        run: python scraper.py

      - name: Generate posts
        run: python generate_posts.py

      - name: Commit and push changes
        run: |
          git config --global user.name "Darvish123"
          git config --global user.email "Alonelahiahdar@gmail.com"
          git add _posts/
          git commit -m "🤖 Hourly auto update" || echo "No changes to commit"
          git push https://Darvish123:${{ secrets.GLOUN }}@github.com/Darvish123/Gloun.git
