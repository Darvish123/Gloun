- name: Commit and push changes
      run: |
        git config --global user.name "Darvish123"
        git config --global user.email "Alonealihader@gmail.com"
        git add _posts/
        git commit -m "⏰ Hourly auto update" || echo "No changes to commit"
        git push https://Darvish123:${{ secrets.GH_TOKEN }}@github.com/Darvish123/Gloun.git HEAD:main
