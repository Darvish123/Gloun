name: Scrape Guardian and Push

on:
  workflow_dispatch:
  schedule:
    - cron: '0 8 * * *' # هر روز ساعت ۸ صبح UTC اجرا میشه

jobs:
  run-scraper:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '20'

      - name: Install dependencies
        run: npm install

      - name: Run scraper script
        run: node guardian-stealth-ultimate-10-dedup.js

      - name: Commit scraped data
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git add .
          git commit -m "🤖 New data scraped on $(date)" || echo "No changes to commit"
          git push